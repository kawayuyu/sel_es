<div class="contents row">
    <div class="content_post" style="background-image: url(<%= asset_path('if.png') %>);"%>
      <div class="more">
        <ul class="more_list">
          <li>
            <%= link_to '編集', edit_rule_path(@rule.id), method: :get %>
          </li>
          <li>
            <%= link_to '削除', rule_path(@rule.id), method: :delete %>
          </li>
        </ul>
      </div>
      <div >
        <ul class="objective-explain-box">
            <li><%= @rule.created_at.strftime('%Y/%m/%d') %></li>
            <br><br>
            <li>【状況（時間と場所を中心に、できる限り具体的に設定）】<br><%= safe_join(@rule.if.split("\n"),tag(:br)) %></li>
            <br><br>
            <li>【行動（場面をイメージした上で、詳細まで具体的に設定）】<br><%= safe_join(@rule.then.split("\n"),tag(:br)) %></li>
            <br><br>
        </ul>
      </div>
    </div>
</div>
<div class="contents row">
<div class="success">
  <%= link_to '習慣一覧へ戻る', rules_path, class: "btn" %>
</div>
</div>